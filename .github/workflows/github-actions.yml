name: assignment-test
on: 
    push:
        tags-ignore:
            - '*'
        branches:
            - '*'
jobs:
    unit-test:
        runs-on: self-hosted
        steps:
          - name: Checkout submodules
            run : echo ""
          - name: Run unit test
            run:  echo ""
    full-test:
        runs-on: self-hosted
        steps:
          - name: Checkout submodules
            run : echo ""
          - name: Run full test
            run:  echo ""
